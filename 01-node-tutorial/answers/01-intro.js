let  num = Math.random();
console.log("The Math.random() method returns a random floating point number between 0 (inclusive) and 1 (exclusive)");
if (num < 0.5){
    console.log(`The Number is less than 0.5: ${num}`);
}
    else {
          console.log(`The number is more than 0.5: ${num}`);
    }
